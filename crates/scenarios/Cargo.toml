[package]
name = "synthetic_pop_scenarios"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
name = "synthetic_pop_scenarios"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
synthetic_pop_core = { path = "../core" }
